streamlit>=1.32.0

# OpenCV headless — Debian Trixie compatible
opencv-python-headless==4.9.0.80

numpy>=1.24.0,<2.0.0
Pillow>=10.0.0

# PyTorch CPU — Streamlit Cloud has no GPU
torch==2.2.2+cpu
torchvision==0.17.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Ultralytics YOLOE
ultralytics>=8.4.0

# CLIP — required by model.set_classes() (text prompt)
git+https://github.com/ultralytics/CLIP.git

# MobileCLIP — required by free-prompt *-pf.pt models
git+https://github.com/ultralytics/mobileclip.git
